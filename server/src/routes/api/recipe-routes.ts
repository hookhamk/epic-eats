import express from 'express';
import type { Request, Response } from 'express';
import { CompactRecipe } from '../../service/recipeService';

const router = express.Router();

//Recipe routes will go here



export {router as recipeRouter};
